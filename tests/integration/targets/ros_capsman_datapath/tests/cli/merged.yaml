# -*- coding: utf-8 -*-
#
# Copyright (C) 2020 Anthonius Munthi
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Ansible RouterOS Module Generator
#     and manual changes will be clobbered when the file is regenerated.
#
#     Please read more about how to change this file at
#     https://github.com/kilip/routeros-generator
#
# ----------------------------------------------------------------------------
---

- include_tasks: pre_tasks.yml

- name: set result commands facts
  set_fact:
    commands: [
      '/caps-man datapath set [ find name=test ] arp=reply-only bridge=br-trunk',
      '/caps-man datapath add arp=reply-only bridge=br-trunk name=new'
    ]

- name: Merge with device configuration
  register: result
  kilip.routeros.ros__capsman_datapath:
    state: merged
    config:
      - name: test
        bridge: br-trunk
        arp: reply-only
      - name: new
        bridge: br-trunk
        arp: reply-only


- name: set diff commands
  set_fact:
    diff_commands: "{{ result.commands | difference(commands) }}"


- assert:
    that:
      - result.changed == True
      - diff_commands == []

- name: "idempotence: Merge with device configuration"
  register: result
  kilip.routeros.ros__capsman_datapath:
    state: merged
    config:
      - name: test
        bridge: br-trunk
        arp: reply-only
      - name: new
        bridge: br-trunk
        arp: reply-only

- assert:
    that:
      - result.changed == False
      - result.commands == []
